import { orThat } from '../../../helpers/or-that';
import { module, test } from 'qunit';

module('Unit | Helper | or that');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = orThat(42);
  assert.ok(result);
});
